<h2>Recently added books</h2>
<div class='container'>
	<% books.forEach(book=>{ %>
		<a href='/books/<%= book.id %>'>
			<img height='150' width='100' src='<%= book.coverImageData %>'>
		</a>
	<% }) %>
</div>